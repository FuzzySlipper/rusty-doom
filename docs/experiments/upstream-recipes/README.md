# Upstream architectural authoring (Engine #8274)

Engine `79e11693bbad110e032ed260a777fb237a7d42a3`, pair `0.1.0-dev.79e11693bbad`, supplies validated `PlanarRecipes.ConvexPrism` and `Walkway`, explicit implicit texture projection, and room shells that emit matching expected joins/openings. Its standalone `fixtures/csharp-architectural-room` shows recessed floors, stepped ceilings, windows, a movable door and passage with free-fly inspection.

Loading Bay now consumes that pair. Local planar shape helpers were removed. The wall boxes that formerly rotated geometry only to orient textures now use explicit U/V mapping; recipe-to-world reflection also reflects those axes. Eight authored `RecipeJoin` declarations live with the owning room recipes and are forwarded into the existing opt-in Engine audit. E1M1 layouts, scanner, materials, opening policy and gameplay remain product-owned. The generic rectangular room helper is demonstrated upstream; irregular E1M1 rooms retain their manual composition.

Verification: matched pair verifier, complete C# spine (semantic catalog, lifecycle, CoreCLR, NativeAOT), Angular build, provenance and boundary checks passed. [Continuity readback](continuity.json): all eight expected joins complete with no gap findings; all three regional enclosures complete with only intentional openings. The four whole-mesh integrity findings match the piece IDs/classifications in `../east-gallery/continuity-after.json` (two OpenBoundary, two NonManifold). They are preexisting findings, not resolved by this authoring migration or proof of visible leaks. Sampling remains bounded by reported resolution.

Native Wolf demo session `a9a4fb29-bbf7-4966-8b5e-5c1006fb6e32` showed textured rooms and normal HUD, movement and pickup continuation. Original captures under `/home/dev/dsh-crew/experiments/wolf-den-srv/controller/state/a9a4fb29-bbf7-4966-8b5e-5c1006fb6e32/`: `0481a91d-0f96-4c7e-814a-e3152b6ef6cc.png` (settled spawn), `b050051b-b544-4a59-8a30-ddabc66490d3.png` (movement/pickup). Initial capture preceded readiness. Reset and session cleanup succeeded. This is focused migration evidence, not full E1M1 certification.

The upstream example was visibly inspected in session `f3c258f2-a75d-4c58-bbbf-d4f4ace2a69a`: `f9481582-2bd1-4bd8-981e-9ca53514c8cc.png` and `3b1d1e5d-8a8c-4e85-a478-01a47a3cabea.png` show the room and opened door/passage. Its final-pair native audit completed eight joins with zero gaps and four IntentionalOpening enclosure findings. Live demo restored at port 4395 with audits and debugger mode disabled; upstream example is available at port 40178. Receipts/logs are in `.runtime/evidence/upstream-recipes/`.

The visible captures and continuity readbacks above used the immediately preceding pair; the final `79e11693bbad` change only derives the Rust projection enum default to satisfy Clippy. The final exact pair passed both CI checks and the complete demo spine. Warning capture completed both browser and Engine streams with no Engine errors; the headless browser reported the GPU ReadPixels stall warning, and no comparison baseline was available. This is not a clean warning-delta claim.
